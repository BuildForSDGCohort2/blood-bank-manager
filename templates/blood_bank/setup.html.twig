<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Setup</title>
</head>
<body>
    <form action="{{ path('blood_bank_setup', {id: bloodbank.id}) }}" method="post">
        {{ form_widget(setupForm) }}

        <button type="submit">Finish</button>
    </form>
</body>
</html>