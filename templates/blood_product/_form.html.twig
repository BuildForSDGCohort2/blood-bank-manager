<form action="{{ path('blood_product_new', {'codeName': app.session.get('bloodBank').codeName}) }}" method="post">
    {{ form_widget(form) }}
    <div class="row mt-2">
        <div class="col">
            <button class="btn btn-primary">{{ button_label|default('Save product') }}</button>
        </div>
    </div>
</form>